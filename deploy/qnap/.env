# =============================================================================
# QNAP Deployment Configuration
# =============================================================================

# Production version — change this to promote a new release to prod
# After changing, run: docker compose up -d
PROD_VERSION=v0.1.0
